<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>netstandard2.1</TargetFramework>
    <Nullable>enable</Nullable>
    <GenerateDocumentationFile>true</GenerateDocumentationFile>
    <PackageId>Moquestra.TypeIds</PackageId>
    <Description>A library for bidirectional mapping between types and integer IDs.</Description>
    <PackageTags>typeid;type-id;type-mapping;dispatch</PackageTags>
  </PropertyGroup>

  <ItemGroup>
    <InternalsVisibleTo Include="Moquestra.TypeIds.Tests" />
  </ItemGroup>

</Project>
